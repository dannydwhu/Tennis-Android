<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 最简化主题 - 兼容鸿蒙 -->
    <style name="Theme.SmartformTennis" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/brand_secondary</item>
    </style>

    <!-- 底部导航指示器样式 -->
    <style name="BottomNav.Indicator" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="android:color">@color/bg_surface</item>
    </style>
</resources>
